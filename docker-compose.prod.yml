# App-only host mysqld connection. The VPS stack lives in yayaue.me/compose.yaml.
services:
  app:
    environment:
      DB_HOST: host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
