services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - "${PCP_PORT:-7144}:7144"
    env_file: .env
    environment:
      TZ: Asia/Tokyo
    volumes:
      - ./peercast-0yp.toml:/app/peercast-0yp.toml:ro
